# 🔀 Fluxo de Dados

Este documento explica como os dados fluem através da aplicação, desde a entrada do usuário até a atualização da UI.

---

## 📚 Índice

1. [Modelo de Fluxo no CakePHP 2](#-modelo-de-fluxo-no-cakephp-2)
2. [Padrão de Atualização](#-padrão-de-atualização)
3. [Comunicação entre Componentes](#-comunicação-entre-componentes)
4. [Fluxos por Feature](#-fluxos-por-feature)
   - [Cadastro de Prestador](#1-cadastro-de-prestador)
   - [Importação CSV](#2-importação-csv)
   - [Busca e Filtros](#3-busca-e-filtros)
   - [Dashboard com Cache](#4-dashboard-com-cache)
5. [Estado Global vs Local](#-estado-global-vs-local)

---

## 🎯 Modelo de Fluxo no CakePHP 2

O sistema utiliza o padrão MVC do CakePHP 2 com camada de serviços:

```
┌─────────────────────────────────────────────────────────────┐
│                    FLUXO DE DADOS                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   HTTP Request → Controller → Service (Facade)               │
│                                                              │
│   Service (Facade) → Query/CRUD Service → Model             │
│                                                              │
│   Model → Database → Model → Service → Controller           │
│                                                              │
│   Controller → View → HTTP Response                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📐 Padrão de Atualização

Todo fluxo de dados segue o mesmo padrão unidirecional:

```
┌─────────────────────────────────────────────────────────────┐
│ 1. USER INPUT (View/Template)                              │
│    Usuário preenche formulário ou clica em ação             │
└───────────────────────────┬─────────────────────────────────┘
                            │ POST/GET
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. CONTROLLER (HTTP Handler)                               │
│    Controller recebe requisição e valida entrada            │
└───────────────────────────┬─────────────────────────────────┘
                            │ service->method()
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. SERVICE FACADE (Coordenação)                            │
│    Facade delega para serviço especializado                 │
└───────────────────────────┬─────────────────────────────────┘
                            │ query/crud service
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. BUSINESS LOGIC (Service Especializado)                   │
│    Service processa regras de negócio e validações          │
└───────────────────────────┬─────────────────────────────────┘
                            │ model->save() / find()
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. MODEL (Acesso a Dados)                                   │
│    Model valida, sanitiza e persiste no banco               │
└───────────────────────────┬─────────────────────────────────┘
                            │ SQL
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. DATABASE (Persistência)                                  │
│    MySQL armazena ou retorna dados                          │
└───────────────────────────┬─────────────────────────────────┘
                            │ dados retornados
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. RESPONSE (Controller → View)                             │
│    Controller formata resposta e renderiza view             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔗 Comunicação entre Componentes

### Controller → Service (Facade)

```php
// ProvidersController.php
public function add() {
    if ($this->request->is('post')) {
        $result = $this->_providerService->create($this->request->data);

        if ($result['success']) {
            $this->Flash->success($result['message']);
            return $this->redirect(array('action' => 'index'));
        }
    }
}
```

### Service Facade → Service Especializado

```php
// ProviderBusinessService.php (Facade)
public function create($data) {
    $result = $this->_crudService->create($data);
    if ($result['success']) {
        $this->_dashboardService->invalidateCache();
    }
    return $result;
}
```

### Service → Model

```php
// ProviderCrudService.php
public function create($data) {
    $this->_Provider->create();
    if ($this->_Provider->saveAssociated($data, array('deep' => true))) {
        return array('success' => true, 'message' => '...');
    }
}
```

---

## 🔄 Fluxos por Feature

### 1. Cadastro de Prestador

```
┌─────────────────────────────────────────────────────────────┐
│ [User] Preenche formulário e envia                          │
└───────────────────────────┬─────────────────────────────────┘
                            │ POST /providers/add
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [ProvidersController] add()                                 │
│   - Valida método HTTP (POST)                               │
│   - Chama ProviderBusinessService->create()                 │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [ProviderBusinessService] create()                          │
│   - Delega para ProviderCrudService->create()               │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [ProviderCrudService] create()                              │
│   - Inicia transação                                        │
│   - Processa nome completo (first_name + last_name)         │
│   - Processa upload de foto (PhotoUploadService)            │
│   - Salva Provider com associações (ProviderService)        │
│   - Commit ou Rollback                                      │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Provider Model] saveAssociated()                           │
│   - Valida dados (beforeSave)                               │
│   - Sanitiza telefone e email                               │
│   - Insere Provider                                         │
│   - Insere ProviderService (associações)                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Controller] Resposta                                       │
│   - Flash message de sucesso/erro                           │
│   - Redirect para index                                     │
└─────────────────────────────────────────────────────────────┘
```

### 2. Importação CSV

```
┌─────────────────────────────────────────────────────────────┐
│ [User] Faz upload de arquivo CSV                            │
└───────────────────────────┬─────────────────────────────────┘
                            │ POST /providers/import
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [ProvidersController] import()                              │
│   - Valida se arquivo foi enviado                           │
│   - Chama CsvImportService->import()                        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [CsvImportService] import()                                 │
│   1. Valida arquivo (CsvFileValidator)                      │
│      - Extensão, MIME type, tamanho                         │
│   2. Parseia CSV                                            │
│      - Detecta delimitador (vírgula/ponto-e-vírgula)        │
│      - Valida header                                        │
│   3. Valida linhas (CsvRowValidator)                        │
│      - Sanitiza dados (XSS)                                 │
│      - Valida campos obrigatórios                           │
│   4. Agrupa por email (múltiplos serviços)                  │
│   5. Processa em transação                                  │
│      - Para cada prestador:                                 │
│        * Verifica se email existe                           │
│        * Cria/find Service pelo nome                        │
│        * Salva Provider com ProviderServices                │
│      - Rollback se >50% falhar                              │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Controller] Resposta                                       │
│   - Flash message com estatísticas                          │
│   - Lista de erros (se houver)                              │
└─────────────────────────────────────────────────────────────┘
```

### 3. Busca e Filtros

```
┌─────────────────────────────────────────────────────────────┐
│ [User] Digita termo de busca e submete                      │
└───────────────────────────┬─────────────────────────────────┘
                            │ GET /providers?search=eletricista
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [ProvidersController] index()                               │
│   - Extrai parâmetros de query                              │
│   - Chama ProviderBusinessService->listWithFilters()        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [ProviderBusinessService] listWithFilters()                 │
│   - Delega para ProviderQueryService->listWithFilters()     │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [ProviderQueryService] listWithFilters()                    │
│   1. Constrói condições de busca                            │
│      - Busca por nome, email, telefone                      │
│      - Busca por serviço (subquery)                         │
│   2. Executa query otimizada                                │
│   3. Ordena resultados (se necessário)                      │
│   4. Aplica paginação                                       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Controller] Renderiza view                                 │
│   - Passa providers, totalCount, paging                     │
│   - View exibe tabela com resultados                        │
└─────────────────────────────────────────────────────────────┘
```

### 4. Dashboard com Cache

```
┌─────────────────────────────────────────────────────────────┐
│ [User] Acessa página inicial                                │
└───────────────────────────┬─────────────────────────────────┘
                            │ GET /
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [HomeController] index()                                    │
│   - Chama DashboardService->getMetrics()                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [DashboardService] getMetrics()                             │
│   1. Verifica cache (15 minutos)                            │
│      - Se existe: retorna cache                             │
│      - Se não: calcula métricas                             │
│   2. Calcula métricas:                                      │
│      - Total de prestadores                                 │
│      - Total de tipos de serviço                            │
│      - Ticket médio                                         │
│      - Serviço mais popular                                 │
│      - Faixa de preços                                      │
│      - Últimos prestadores                                  │
│      - Dados para gráfico                                   │
│   3. Salva no cache                                         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Controller] Renderiza view                                 │
│   - View exibe cards com métricas                           │
│   - Chart.js renderiza gráfico                              │
└─────────────────────────────────────────────────────────────┘
```

**Invalidação de Cache:**
- Quando Provider é criado/atualizado/excluído
- Quando CSV é importado
- Cache invalidado via `DashboardService->invalidateCache()`

---

## 📦 Estado Global vs Local

### Estado Global (Cache)

```php
// DashboardService.php
Cache::write('dashboard_metrics', $metrics, 'dashboard');
$cached = Cache::read('dashboard_metrics', 'dashboard');
```

**Uso:**
- Métricas do dashboard (15 minutos de TTL)
- Dados que não mudam frequentemente
- Performance de consultas pesadas

### Estado Local (Request)

```php
// Controller
$this->set('providers', $result['providers']);
$this->set('search', $this->request->query('search'));
```

**Uso:**
- Dados específicos da requisição atual
- Parâmetros de busca/filtro
- Dados temporários de formulários

### Estado de Sessão (Flash Messages)

```php
// Controller
$this->Flash->success('Prestador salvo com sucesso.');
```

**Uso:**
- Mensagens de feedback entre requisições
- Gerenciado pelo CakePHP FlashComponent

---

## 🔗 Próximos Documentos

- [REFERENCE.md](./REFERENCE.md) - Referência completa de APIs
- [ARCHITECTURE.md](./ARCHITECTURE.md) - Visão arquitetural
- [SPECIFICATION.md](./SPECIFICATION.md) - Especificação completa

